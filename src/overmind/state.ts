type State = {
  serverMessage: string;
  componentMessage: string;
  onInitializeMessage: string;
};

export const state: State = {
  serverMessage: '',
  componentMessage: '',
  onInitializeMessage: '',
};
