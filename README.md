# Overmind JS SSR Actions Bug

Actions are undefined on the server, which makes them difficult to use - have to be very careful.

```
yarn
yarn start
```

Note terminal error messages when loading page at `http://localhost:3000`.
